columns:
    id:
        label: id
        type: number
    order_question:
        label: 'Order question'
        relation: order_question
        select: regexp_replace(question, E'<[^>]+>', '', 'gi')
    answer:
        label: answer
        type: text
    order:
        label: order
        type: text
